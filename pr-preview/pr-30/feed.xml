<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="3.10.0">Jekyll</generator><link href="https://lalet.me/pr-preview/pr-30/feed.xml" rel="self" type="application/atom+xml" /><link href="https://lalet.me/pr-preview/pr-30/" rel="alternate" type="text/html" /><updated>2026-07-01T18:45:15+00:00</updated><id>https://lalet.me/pr-preview/pr-30/feed.xml</id><title type="html">Lalet Scaria</title><subtitle>Staff Engineer (Platform/Reliability) — notes on cloud platforms, Kubernetes, and reliability.</subtitle><author><name>Lalet Scaria</name></author><entry><title type="html">Give Yourself a Chat Bot: OpenRouter + OpenClaw + Telegram</title><link href="https://lalet.me/pr-preview/pr-30/2026/07/01/openrouter-openclaw-telegram-setup.html" rel="alternate" type="text/html" title="Give Yourself a Chat Bot: OpenRouter + OpenClaw + Telegram" /><published>2026-07-01T00:00:00+00:00</published><updated>2026-07-01T00:00:00+00:00</updated><id>https://lalet.me/pr-preview/pr-30/2026/07/01/openrouter-openclaw-telegram-setup</id><content type="html" xml:base="https://lalet.me/pr-preview/pr-30/2026/07/01/openrouter-openclaw-telegram-setup.html"><![CDATA[<p>I wanted an AI assistant I could just text like a friend. Not a browser tab, not an app, just Telegram. Turns out you can wire that up in about twenty minutes with two free accounts and one open-source project. Here’s the whole thing, start to finish.</p>

<h2 id="what-youre-building">What you’re building</h2>

<p><a href="https://openclaw.ai/">OpenClaw</a> is a self-hosted AI assistant that runs as its own service and connects to chat apps (Telegram, WhatsApp, Slack, and a bunch more). <a href="https://openrouter.ai/">OpenRouter</a> sits in front of dozens of AI models and gives you one API key that works with all of them, including several that cost nothing at all. Point OpenClaw at OpenRouter, wire up a Telegram bot, and you’ve got a chat assistant with zero monthly bill.</p>

<figure>
  <img src="/pr-preview/pr-30/assets/images/openrouter-activity.png" alt="OpenRouter activity dashboard showing $0.00 total spend, 67 requests, and 2.53M tokens used" loading="lazy" />
  <figcaption>My actual usage after a week of poking at this. $0.00 spent.</figcaption>
</figure>

<h2 id="what-you-need">What you need</h2>

<ul>
  <li>Somewhere to run OpenClaw: a spare server, a VM, a container, anything that can run Node. I run mine in a Kubernetes pod, but none of the steps below care where it lives.</li>
  <li>A free <a href="https://openrouter.ai/">OpenRouter</a> account.</li>
  <li>A Telegram account.</li>
</ul>

<h2 id="step-1-get-an-openrouter-key-and-pick-a-free-model-on-purpose">Step 1: Get an OpenRouter key, and pick a free model on purpose</h2>

<p>Sign up at OpenRouter, then grab an API key from your account settings. Free, no card needed.</p>

<p>Here’s the part that bit me: OpenRouter has a model called <code class="language-plaintext highlighter-rouge">auto</code> that picks whichever model it thinks is best for your prompt. Sounds convenient. The problem is <code class="language-plaintext highlighter-rouge">auto</code> isn’t guaranteed to stay inside the free tier, so on a zero-balance account it can quietly route you to a paid model and then fail with “insufficient balance” the moment it does.</p>

<p>Skip <code class="language-plaintext highlighter-rouge">auto</code>. Pick an explicit free model instead, something with a <code class="language-plaintext highlighter-rouge">:free</code> suffix. You can browse the current list at <a href="https://openrouter.ai/models?max_price=0">openrouter.ai/models, filtered to zero cost</a>. I’m running <code class="language-plaintext highlighter-rouge">nvidia/nemotron-3-ultra-550b-a55b:free</code>, a 1M-context model with tool-calling support, which matters if you want your assistant to actually do things and not just chat.</p>

<p>Free models are capped at 50 requests a day and 20 a minute on a zero-balance account. If that’s tight, a one-time $10 top-up permanently raises the cap to 1000 a day, whether or not you ever spend the rest of that balance on anything else.</p>

<h2 id="step-2-install-openclaw-and-point-it-at-openrouter">Step 2: Install OpenClaw and point it at OpenRouter</h2>

<p>Follow OpenClaw’s own <a href="https://docs.openclaw.ai/">install docs</a> for your platform. Once it’s running, you need to tell it two things: which model to use, and where your OpenRouter key lives.</p>

<p>If you’re running it somewhere without an interactive terminal (a container, a headless server), OpenClaw’s normal setup wizard won’t work, since it needs a TTY. Use its config command instead:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>openclaw config <span class="nb">set</span> <span class="nt">--replace</span> <span class="nt">--batch-json</span> <span class="s1">'[
  {"path":"gateway.mode","value":"local"},
  {"path":"agents.defaults.model.primary","value":"openrouter/nvidia/nemotron-3-ultra-550b-a55b:free"}
]'</span>
</code></pre></div></div>

<p>And set your key as an environment variable wherever OpenClaw runs:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">export </span><span class="nv">OPENROUTER_API_KEY</span><span class="o">=</span><span class="s2">"your-key-here"</span>
</code></pre></div></div>

<p>No extra provider config needed. OpenRouter is a built-in provider; the env var and the model name above are all it takes.</p>

<h2 id="step-3-make-a-telegram-bot">Step 3: Make a Telegram bot</h2>

<p>Open Telegram, search for <a href="https://t.me/BotFather">@BotFather</a>, and send it <code class="language-plaintext highlighter-rouge">/newbot</code>. Answer the two questions it asks (a display name, then a username ending in <code class="language-plaintext highlighter-rouge">bot</code>), and it’ll hand you back a token that looks like <code class="language-plaintext highlighter-rouge">123456789:ABCdefGhIJKlmNoPQRsTUVwxyZ</code>. That’s your bot’s password. Don’t post it anywhere public.</p>

<h2 id="step-4-wire-the-bot-token-in-and-turn-the-channel-on">Step 4: Wire the bot token in and turn the channel on</h2>

<p>Same pattern as the model config:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">export </span><span class="nv">TELEGRAM_BOT_TOKEN</span><span class="o">=</span><span class="s2">"your-bot-token-here"</span>

openclaw config <span class="nb">set</span> <span class="nt">--replace</span> <span class="nt">--batch-json</span> <span class="s1">'[
  {"path":"channels.telegram.enabled","value":true},
  {"path":"channels.telegram.groupPolicy","value":"disabled"}
]'</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">groupPolicy: disabled</code> just means this bot only talks to you in DMs, not in group chats. Leave that off if you want group support later.</p>

<p>Restart OpenClaw so the new config takes effect.</p>

<h2 id="step-5-say-hello-then-approve-yourself">Step 5: Say hello, then approve yourself</h2>

<p>Open Telegram, find the bot you just created, and send it any message, <code class="language-plaintext highlighter-rouge">/start</code> works fine. It won’t reply yet. Behind the scenes it just generated a pairing request, and approving that is a one-time command, not a button in the chat:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>openclaw pairing list telegram
</code></pre></div></div>

<p>That prints a short code next to your Telegram user ID. Approve it:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>openclaw pairing approve telegram YOUR_CODE_HERE
</code></pre></div></div>

<p>That’s it. You’re paired, and if this is the first person ever approved, OpenClaw also makes you the bot’s owner.</p>

<h2 id="step-6-talk-to-it">Step 6: Talk to it</h2>

<p>Send the bot a real message. You should get a real reply, generated by the free model on OpenRouter, delivered straight to your phone.</p>

<figure>
  <img src="/pr-preview/pr-30/assets/images/telegram-chat.png" alt="A phone screenshot of a Telegram chat with a bot named Rocky, replying with real content" loading="lazy" />
  <figcaption>Mine, replying from my phone. It'll only know what you tell it.</figcaption>
</figure>

<h2 id="snags-i-actually-hit">Snags I actually hit</h2>

<p>If you get an “insufficient balance” error out of nowhere, you’re probably still on <code class="language-plaintext highlighter-rouge">openrouter/auto</code> and it picked a paid model without asking. Pin an explicit <code class="language-plaintext highlighter-rouge">:free</code> model instead (Step 1).</p>

<p>Rate limit errors mean you’ve gone past the free tier’s 50 requests a day or 20 a minute. Slow down, or do the $10 top-up.</p>

<p>If your config seems to reset itself after a restart, you’re writing it somewhere that doesn’t survive a container restart. Move it to persistent storage.</p>

<p>And if the bot stays silent after <code class="language-plaintext highlighter-rouge">/start</code>, that’s normal. Pairing is a manual step, not automatic. Run <code class="language-plaintext highlighter-rouge">openclaw pairing list telegram</code> and approve your code.</p>

<p>That’s the whole setup. No monthly bill, and a bot in my pocket that only knows what I told it.</p>]]></content><author><name>Lalet Scaria</name></author><category term="openclaw" /><category term="openrouter" /><category term="telegram" /><category term="selfhosted" /><category term="ai" /><summary type="html"><![CDATA[I wanted an AI assistant I could just text like a friend. Not a browser tab, not an app, just Telegram. Turns out you can wire that up in about twenty minutes with two free accounts and one open-source project. Here’s the whole thing, start to finish.]]></summary></entry><entry><title type="html">Workload Identity for Pods: A SPIFFE/SPIRE Primer</title><link href="https://lalet.me/pr-preview/pr-30/2026/07/01/spiffe-spire-mutual-auth.html" rel="alternate" type="text/html" title="Workload Identity for Pods: A SPIFFE/SPIRE Primer" /><published>2026-07-01T00:00:00+00:00</published><updated>2026-07-01T00:00:00+00:00</updated><id>https://lalet.me/pr-preview/pr-30/2026/07/01/spiffe-spire-mutual-auth</id><content type="html" xml:base="https://lalet.me/pr-preview/pr-30/2026/07/01/spiffe-spire-mutual-auth.html"><![CDATA[<p>Most Kubernetes network policy answers one question: can this IP reach that IP, on this port. It never asks who’s actually on the other end. <a href="https://spiffe.io/">SPIFFE</a> and <a href="https://spiffe.io/docs/latest/spire-about/spire-concepts/">SPIRE</a> exist to answer that second question, and I ended up wiring them into my own cluster to gate a specific pod-to-pod link. Here’s what they are, and how the build went.</p>

<figure>
  <img src="https://media.giphy.com/media/v1.Y2lkPTc5MGI3NjExZ3lpdGhleHp0dzNncmYzYWhkZzgxZXZ6cnd1YW9wcWMzN2F5czNpeiZlcD12MV9naWZzX3NlYXJjaCZjdD1n/l4dLyz1rYq1eK2AIsD/giphy.gif" alt="A bouncer standing with arms crossed, guarding a door" loading="lazy" />
  <figcaption>Basically what mutual authentication does at every hop, minus the beret.</figcaption>
</figure>

<h2 id="the-problem-with-ip-based-trust">The problem with IP-based trust</h2>

<p>A Kubernetes <code class="language-plaintext highlighter-rouge">NetworkPolicy</code> (or its Cilium equivalent) typically allows traffic based on labels and namespaces, which the control plane resolves down to IP addresses under the hood. That works, but it’s trusting the network, not the workload. If something compromises a node or spoofs a source IP, a policy built purely on “traffic from this subnet is fine” has nothing left to check.</p>

<p>SPIFFE flips that around. Every workload gets a cryptographic identity, independent of where it happens to be running, and services can require proof of that identity before talking to each other. It’s the same idea behind mutual TLS, standardized so any workload orchestrator can plug into it.</p>

<figure>
  <img src="https://media.giphy.com/media/v1.Y2lkPWVjZjA1ZTQ3bGxucW0wdWNxcTMycWEyNDVpOWRmNTR4N2J0djQ4dWxsanN6Y3pkeCZlcD12MV9naWZzX3NlYXJjaCZjdD1n/lL7kQLUCBD7a4LGwZS/giphy.gif" alt="Line drawing of a dog with the caption 'IAM is hard.' and hashtag HeckinIAM" loading="lazy" />
  <figcaption>Identity and access management in one honest sentence.</figcaption>
</figure>

<h2 id="what-spiffe-and-spire-actually-are">What SPIFFE and SPIRE actually are</h2>

<p><a href="https://spiffe.io/docs/latest/spiffe-about/spiffe-concepts/">SPIFFE</a> is a specification: it defines a URI format for workload identity (a <strong>SPIFFE ID</strong>, like <code class="language-plaintext highlighter-rouge">spiffe://example.org/my-service</code>) and a document format for proving it (an <strong>SVID</strong>, a short-lived X.509 certificate or JWT that attests “I am this identity, signed by an authority you trust”). It doesn’t run anything by itself.</p>

<p><a href="https://spiffe.io/docs/latest/spire-about/spire-concepts/">SPIRE</a> is the reference implementation. It has a server that acts as the certificate authority for a <strong>trust domain</strong>, and an agent on each node that figures out what’s actually running there (which pod, which service account, which container image) and matches it against registered rules. If a workload’s real, observed attributes match a rule, the agent hands it a short-lived SVID. No shared secret gets baked into an image or mounted as a static file. The certificate is minted just for that workload, at runtime, based on what it verifiably is.</p>

<h2 id="where-cilium-fits-in">Where Cilium fits in</h2>

<p>I run <a href="https://cilium.io/">Cilium</a> for cluster networking already, and it has a built-in integration that uses SPIRE as its identity backend: <a href="https://docs.cilium.io/en/stable/network/servicemesh/mutual-authentication/mutual-authentication/">mutual authentication</a>. Enable it, and a <code class="language-plaintext highlighter-rouge">CiliumNetworkPolicy</code> rule can carry an <code class="language-plaintext highlighter-rouge">authentication.mode: required</code> flag. Traffic covered by that rule only passes once both ends have proven their SPIFFE identity to each other. Everything else in the cluster keeps working exactly as before, since the requirement is opt-in per rule, not global.</p>

<p>Enabling the feature deploys a SPIRE server and an agent on every node. The server issues identities; the agents are the local point of contact each node’s workloads talk to when they need one.</p>

<h2 id="building-it-for-a-real-link">Building it for a real link</h2>

<p>I used this to gate traffic between my self-hosted AI assistant and a local model backend it talked to. The assistant has broad capabilities (shell access, file access), so I wanted every hop it makes to be deliberate and provable, not just “same cluster, must be fine.”</p>

<p>The rollout was mostly turning the feature on and writing one policy rule with <code class="language-plaintext highlighter-rouge">authentication.mode: required</code> on it. The two gotchas that actually cost time were both about visibility and staleness, not the core mechanism:</p>

<ul>
  <li>The identity registrar’s logs were, by default, invisible at the normal log level. I had to bump verbosity temporarily to actually see registration events happening, confirm it, then turn it back down.</li>
  <li>One identity got stuck unregistered, and a plain restart of the component responsible didn’t fix it. It needed a forced leadership handoff to trigger a genuinely fresh resync, not just a restart of the same instance.</li>
</ul>

<p>Neither is a flaw in the design so much as normal early-days friction with a feature that’s still marked beta upstream. Once past that, the behavior was exactly as advertised: kill the identity provider mid-flight, and covered traffic fails closed rather than falling back to trusting the network.</p>

<figure>
  <img src="https://media.giphy.com/media/v1.Y2lkPTc5MGI3NjExaGMwYnA3d3didG9xa3FvM3lqM3k5ajB2cjVxYWgzbWxseGNzZGhkaiZlcD12MV9naWZzX3NlYXJjaCZjdD1n/pgcz9Arntj2Zq/giphy.gif" alt="A black cat filing its claws with a nail file" loading="lazy" />
  <figcaption>Me, sharpening my policy rules after the second gotcha.</figcaption>
</figure>

<h2 id="why-bother">Why bother</h2>

<p>The honest case for this is defense in depth, not paranoia. Namespace isolation and network policy already cover most of what a homelab needs. SPIFFE/SPIRE is for the specific case where you have one link you want to be genuinely sure about, cryptographically, not just topologically, and you’re willing to run the extra control plane to get it. For everything else, a solid default-deny policy and a service account with no permissions does most of the actual work.</p>

<figure>
  <img src="https://media.giphy.com/media/v1.Y2lkPTc5MGI3NjExaGMwYnA3d3didG9xa3FvM3lqM3k5ajB2cjVxYWgzbWxseGNzZGhkaiZlcD12MV9naWZzX3NlYXJjaCZjdD1n/WSrPRXyADD2SUpb5in/giphy.gif" alt="A finger poking a black paw, claws extending in response" loading="lazy" />
  <figcaption>The claws only need to come out for the link that actually matters.</figcaption>
</figure>

<h2 id="references">References</h2>

<ul>
  <li><a href="https://spiffe.io/">SPIFFE overview</a></li>
  <li><a href="https://spiffe.io/docs/latest/spiffe-about/spiffe-concepts/">SPIFFE concepts</a></li>
  <li><a href="https://spiffe.io/docs/latest/spire-about/spire-concepts/">SPIRE concepts</a></li>
  <li><a href="https://docs.cilium.io/en/stable/network/servicemesh/mutual-authentication/mutual-authentication/">Cilium mutual authentication</a></li>
</ul>]]></content><author><name>Lalet Scaria</name></author><category term="cilium" /><category term="spiffe" /><category term="spire" /><category term="kubernetes" /><category term="security" /><summary type="html"><![CDATA[Most Kubernetes network policy answers one question: can this IP reach that IP, on this port. It never asks who’s actually on the other end. SPIFFE and SPIRE exist to answer that second question, and I ended up wiring them into my own cluster to gate a specific pod-to-pod link. Here’s what they are, and how the build went.]]></summary></entry><entry><title type="html">Homelab, Part 8: The TLDR</title><link href="https://lalet.me/pr-preview/pr-30/2026/06/30/homelab-part-8-the-tldr.html" rel="alternate" type="text/html" title="Homelab, Part 8: The TLDR" /><published>2026-06-30T00:00:00+00:00</published><updated>2026-06-30T00:00:00+00:00</updated><id>https://lalet.me/pr-preview/pr-30/2026/06/30/homelab-part-8-the-tldr</id><content type="html" xml:base="https://lalet.me/pr-preview/pr-30/2026/06/30/homelab-part-8-the-tldr.html"><![CDATA[<p>Seven parts in, this thing is real and running, so here is the short version. One photo, one map, and a line on what each box actually does. The full story is linked at the bottom.</p>

<figure>
  <img src="/pr-preview/pr-30/assets/images/homelab-hardware.jpg" alt="Two Raspberry Pi 5s on PoE hats, two Dell OptiPlex 3070 Micro PCs, and a Buffalo NAS on a shelf" loading="lazy" />
  <figcaption>The entire "datacenter". Two Raspberry Pi 5s on PoE hats, two Dell OptiPlex 3070 Micros, and a Buffalo NAS on the shelf below. The whole shelf pulls less power than a desktop at idle.</figcaption>
</figure>

<h2 id="what-it-actually-is">What it actually is</h2>

<p>Seven <a href="https://www.talos.dev/">Talos Linux</a> nodes make one Kubernetes cluster: three control plane, four workers, spread across <a href="https://www.proxmox.com/">Proxmox</a> VMs on the two Dells and the bare Raspberry Pis. <a href="https://cilium.io/">Cilium</a> does the networking. No SSH, no package manager, no pet servers. If a node dies I reflash it and it rejoins.</p>

<figure>
  <img src="/pr-preview/pr-30/assets/images/homelab-architecture.png" alt="Architecture diagram: clients to Cloudflare Zero Trust, into the Talos cluster grouped by GitOps, Platform and Identity, Observability, Applications and Storage, with the NAS and VPN outside" loading="lazy" />
  <figcaption>Same stack, drawn out. Clients on the left, the cluster in the middle, the NAS and the VPN on the outside.</figcaption>
</figure>

<h2 id="what-runs-on-it">What runs on it</h2>

<p>Everything below lives as YAML in one git repo. <a href="https://argo-cd.readthedocs.io/">ArgoCD</a> watches that repo and makes the cluster match it, and <a href="https://github.com/getsops/sops">SOPS</a> with an age key keeps the secrets encrypted right next to the code. I change the homelab by opening a pull request.</p>

<ul>
  <li><strong>Login.</strong> <a href="https://goauthentik.io/">Authentik</a> is the single sign-on, sitting behind <a href="https://www.cloudflare.com/zero-trust/">Cloudflare Zero Trust</a>. One account gets me into Grafana, ArgoCD, and the dashboard, with a one-time PIN as backup.</li>
  <li><strong>Watching it.</strong> <a href="https://victoriametrics.com/">VictoriaMetrics</a> for metrics, <a href="https://grafana.com/oss/loki/">Loki</a> plus Alloy for logs, <a href="https://github.com/cilium/hubble">Hubble</a> for who-talks-to-who, all drawn in <a href="https://grafana.com/">Grafana</a>.</li>
  <li><strong>Media.</strong> <a href="https://jellyfin.org/">Jellyfin</a> transcodes on the Intel iGPU in one of the Dells. A download stack feeds it, and torrents only leave through a VPN that kills the connection if the tunnel drops.</li>
  <li><strong>Storage.</strong> Three tiers. The Buffalo NAS serves bulk files over NFS, <a href="https://longhorn.io/">Longhorn</a> does replicated block for databases, and node-local SSD handles anything that hates a network mount. <a href="https://garagehq.deuxfleurs.fr/">Garage</a> covers S3 objects.</li>
</ul>

<h2 id="the-three-rules-i-never-broke">The three rules I never broke</h2>

<ol>
  <li>If it is not in git, it does not exist.</li>
  <li>No secret in plaintext, anywhere, ever.</li>
  <li>Nothing is reachable without a login in front of it. The one exception is the TV talking to Jellyfin over the LAN.</li>
</ol>

<p>That is the homelab. Small shelf, boring rules, and I can rebuild the whole thing from a git clone.</p>

<h2 id="the-long-version">The long version</h2>

<ul>
  <li><a href="/pr-preview/pr-30/2026/06/21/homelab-part-1-hardware.html">Part 1: Hardware</a></li>
  <li><a href="/pr-preview/pr-30/2026/06/22/homelab-part-2-proxmox.html">Part 2: Proxmox</a></li>
  <li><a href="/pr-preview/pr-30/2026/06/22/homelab-part-3-kubernetes.html">Part 3: Kubernetes</a></li>
  <li><a href="/pr-preview/pr-30/2026/06/23/homelab-part-4-argocd.html">Part 4: ArgoCD</a></li>
  <li><a href="/pr-preview/pr-30/2026/06/24/homelab-part-5-remote-access.html">Part 5: Remote access</a></li>
  <li><a href="/pr-preview/pr-30/2026/06/24/homelab-part-6-observability.html">Part 6: Observability</a></li>
  <li><a href="/pr-preview/pr-30/2026/06/25/homelab-part-7-single-sign-on.html">Part 7: Single sign-on</a></li>
</ul>]]></content><author><name>Lalet Scaria</name></author><category term="homelab" /><category term="kubernetes" /><category term="talos" /><category term="gitops" /><category term="overview" /><summary type="html"><![CDATA[Seven parts in, this thing is real and running, so here is the short version. One photo, one map, and a line on what each box actually does. The full story is linked at the bottom.]]></summary></entry><entry><title type="html">Homelab, Part 7: One Login for All of It</title><link href="https://lalet.me/pr-preview/pr-30/2026/06/25/homelab-part-7-single-sign-on.html" rel="alternate" type="text/html" title="Homelab, Part 7: One Login for All of It" /><published>2026-06-25T00:00:00+00:00</published><updated>2026-06-25T00:00:00+00:00</updated><id>https://lalet.me/pr-preview/pr-30/2026/06/25/homelab-part-7-single-sign-on</id><content type="html" xml:base="https://lalet.me/pr-preview/pr-30/2026/06/25/homelab-part-7-single-sign-on.html"><![CDATA[<p><a href="/pr-preview/pr-30/2026/06/24/homelab-part-6-observability.html">Part 6</a>
gave me a wall of graphs. Useful, but it also rubbed in a problem I’d been
ignoring: every app in the cluster had its own login. Grafana, ArgoCD, the
Proxmox boxes, soon a pile of others. A different password prompt each time, or
worse, the same password everywhere. I wanted what work has: one login, and a
single page of tiles to launch everything from.</p>

<p>That turned into two pieces. A dashboard to look at, and an identity provider
behind it to do the actual logging in.</p>

<h2 id="the-easy-half-a-dashboard">The easy half: a dashboard</h2>

<p>The launcher was the quick win. I went with <a href="https://gethomepage.dev/">Homepage</a>,
which keeps its whole config in plain YAML files in the repo. That mattered more
to me than looks. The popular alternative, Homarr, stores everything in a
database you click together in a browser, which fights the way the rest of this
homelab works: everything is a file in git, applied by
<a href="https://argo-cd.readthedocs.io/">ArgoCD</a>. Homepage just reads config I commit,
so it fits right in.</p>

<p>A handful of tiles later (ArgoCD, the Proxmox nodes, Grafana, and the new login
portal), plus a small widget showing each node’s CPU and memory, and I had a
front page for the whole house.</p>

<figure>
  <img src="https://media.giphy.com/media/2QSvHa1pqlcmBOeI77/giphy.gif" alt="woman doing a chef's kiss with MAGNIFICENT caption" loading="lazy" />
  <figcaption>One page, every box. Should have done this months ago.</figcaption>
</figure>

<h2 id="the-hard-half-actual-single-sign-on">The hard half: actual single sign-on</h2>

<p>A page of links is not single sign-on, though. Clicking a tile still drops you
at that app’s own login. For one real login across everything I needed an
identity provider, and I went with <a href="https://goauthentik.io/">Authentik</a>.</p>

<p>Authentik wants a real database, so it got one: a Postgres cluster managed by
<a href="https://cloudnative-pg.io/">CloudNativePG</a>, the operator that treats a database
as something Kubernetes looks after, failover and all, instead of a pet I
hand-feed. I skipped the database the Authentik chart bundles on purpose. It
ships a vendor image that went legacy last year, and I don’t want to build on
something already rotting. Redis tagged along too, the small throwaway kind,
just a cache.</p>

<h2 id="wiring-it-to-the-front-door">Wiring it to the front door</h2>

<p>Here’s the part I actually enjoyed. Since Part 5, everything reaches the outside
world through <a href="https://developers.cloudflare.com/cloudflare-one/policies/access/">Cloudflare Access</a>,
which until now logged me in by emailing a one-time code. Instead of ripping
that out, I taught it to also offer Authentik as a login option. Visit the
dashboard now and Cloudflare shows a chooser: sign in with Authentik, or get a
code by email.</p>

<p>That second option staying put is deliberate, and it’s the whole trick.</p>

<figure>
  <img src="https://media.giphy.com/media/LRVnPYqM8DLag/giphy.gif" alt="Jordan Peele sweating nervously" loading="lazy" />
  <figcaption>Authentik is down and it's the only way in. Start sweating.</figcaption>
</figure>

<p>If I made Authentik the only way in, then the day it breaks I’m locked out of
ArgoCD, which is the exact tool I’d use to fix Authentik. So the rule I’m
keeping: never put the thing that repairs your login behind your login. The
admin tools keep the email-code door as break-glass. The convenience apps can
hide behind Authentik all they like.</p>

<h2 id="what-bit-me">What bit me</h2>

<p>It was not smooth. A few favorites.</p>

<h3 id="the-read-only-dashboard">The read-only dashboard</h3>

<p>Homepage came up and returned an error on every single page. The logs showed it
trying to create a folder for its own logs and failing. I’d mounted its config
straight from the file in git, which makes that folder read-only, and the app
wants to write there on boot. The fix was to copy the config into a writable
scratch space at startup instead of mounting it frozen.</p>

<h3 id="the-graphs-with-no-numbers">The graphs with no numbers</h3>

<p>The node CPU and memory widget just said “API Error.” Homepage asks the cluster
for live usage numbers, and nothing in mine was producing them.
<a href="https://www.talos.dev/">Talos</a> doesn’t ship the little component that does
(<a href="https://github.com/kubernetes-sigs/metrics-server">metrics-server</a>), and when
you add it by hand it needs one extra flag to trust Talos’s self-signed internal
certificates. One flag, and the bars filled in.</p>

<figure>
  <img src="https://media.giphy.com/media/oaXRLIMnkGBnuQOBzE/giphy.gif" alt="thousand yard stare" loading="lazy" />
  <figcaption>Reading scrape configs for an hour when the answer was "install the thing that makes the numbers."</figcaption>
</figure>

<h3 id="the-logo-that-wasnt">The logo that wasn’t</h3>

<p>A small one. The ArgoCD tile showed a broken image while every other icon
loaded. The icon set calls it argo-cd, and I’d written argocd. One hyphen.</p>

<figure>
  <img src="https://media.giphy.com/media/3xz2BLBOt13X9AgjEA/giphy.gif" alt="Batman facepalm" loading="lazy" />
  <figcaption>An hour of my life, one missing hyphen.</figcaption>
</figure>

<h3 id="the-dns-that-lied">The DNS that lied</h3>

<p>The dashboard refused to load with a name-not-found error, while every tool I
checked swore the name resolved fine. The record was new, and I’d visited the
address a few times before it existed, so the browser had cached the “doesn’t
exist” answer. Clearing that cache, not the actual DNS, was the fix.</p>

<h2 id="how-much-backup-is-enough">How much backup is enough?</h2>

<p>Once real logins depended on Authentik, a single database pod felt nervous. If
its node rebooted mid-update, nobody logs in. So I gave it a standby: two
database instances, one leading and one following, with automatic promotion if
the leader falls over.</p>

<p>I almost reached for three. But CloudNativePG doesn’t need a third for a clean
handoff. It leans on Kubernetes itself to decide who leads, so there’s no tie to
break, and a third instance is just a spare for the spare. For a home setup
that’s a pod and a disk I don’t need. Two is the honest amount of paranoia here.</p>

<h2 id="whats-next">What’s next</h2>

<p>The tiles still drop you at each app’s own login for now. The next round is
teaching the apps themselves, Grafana and ArgoCD, to trust Authentik directly,
so one login truly carries everywhere. The fix-it tools keep their break-glass
door. That’s the next post.</p>]]></content><author><name>Lalet Scaria</name></author><category term="homelab" /><category term="kubernetes" /><category term="authentik" /><category term="sso" /><category term="cloudflare" /><summary type="html"><![CDATA[Part 6 gave me a wall of graphs. Useful, but it also rubbed in a problem I’d been ignoring: every app in the cluster had its own login. Grafana, ArgoCD, the Proxmox boxes, soon a pile of others. A different password prompt each time, or worse, the same password everywhere. I wanted what work has: one login, and a single page of tiles to launch everything from.]]></summary></entry><entry><title type="html">Homelab, Part 5: Letting the Outside In</title><link href="https://lalet.me/pr-preview/pr-30/2026/06/24/homelab-part-5-remote-access.html" rel="alternate" type="text/html" title="Homelab, Part 5: Letting the Outside In" /><published>2026-06-24T00:00:00+00:00</published><updated>2026-06-24T00:00:00+00:00</updated><id>https://lalet.me/pr-preview/pr-30/2026/06/24/homelab-part-5-remote-access</id><content type="html" xml:base="https://lalet.me/pr-preview/pr-30/2026/06/24/homelab-part-5-remote-access.html"><![CDATA[<p><a href="/pr-preview/pr-30/2026/06/23/homelab-part-4-argocd.html">Part 4</a> got the
cluster running itself, but it only lived on the home network. I wanted to check
it from the couch, from my phone, from a cafe on borrowed wifi. So I set up two
ways in, for two different jobs, and neither one opens a port on the home router.
That last part is the whole reason I can sleep at night.</p>

<figure>
  <img src="https://media1.giphy.com/media/v1.Y2lkPTc5MGI3NjExZ2FqOHo0dGxidmtwejJ6YTl1anYxMmtnaWo5NDVlM2Jna213ZmYxaiZlcD12MV9pbnRlcm5hbF9naWZfYnlfaWQmY3Q9Zw/NTur7XlVDUdqM/giphy.gif" alt="reaction gif" loading="lazy" />
  <figcaption>The cluster, now reachable from a coffee shop.</figcaption>
</figure>

<h2 id="door-one-dashboards-from-any-browser">Door one: dashboards from any browser</h2>

<p>Sometimes I just want to pull up a web UI on whatever machine is in front of me,
with no client to install. For that I use a
<a href="https://developers.cloudflare.com/cloudflare-one/connections/connect-networks/">Cloudflare Tunnel</a>.</p>

<p>A small agent runs inside the cluster and dials out to Cloudflare, holding the
connection open from the inside. Nothing listens on the router, and the home
address stays hidden. That agent can reach both the in-cluster services and the
other boxes on the network, so there’s no load balancer or port forwarding to
babysit.</p>

<p>The thing that got me: my home gateway quietly blocks the fast UDP protocol the
tunnel prefers, so it just would not connect, with no useful error to explain
why. I stared at it for an embarrassingly long time. The fix was forcing the
tunnel onto the older TCP path, and then it came right up.</p>

<figure>
  <img src="https://media.giphy.com/media/v1.Y2lkPTc5MGI3NjExOHFuOXp0cXkxNThlZWt4bGhiaGl4N2VvdWE2NG9weHNvYjFvZDhjaiZlcD12MV9naWZzX3NlYXJjaCZjdD1n/pUeXcg80cO8I8/giphy.gif" alt="reaction gif" loading="lazy" />
  <figcaption>Me, watching the tunnel fail with no error message.</figcaption>
</figure>

<h2 id="the-guard">The guard</h2>

<p>Public access is only fine if something checks who you are. So the tunnel sits
behind <a href="https://developers.cloudflare.com/cloudflare-one/policies/access/">Cloudflare Access</a>,
which authenticates you at the edge, before any traffic reaches the cluster: an
email code, or a real identity provider. Think of it as a bouncer who works the
door before anyone even reaches the building.</p>

<p>One honest caveat: Access only gets you to the door. The apps still have their own
login behind it, so for now I get carded twice. Making one login cover everything
means wiring each app to an identity provider, which is
<a href="https://goauthentik.io/">Authentik</a>, and that’s a job for later.</p>

<h2 id="door-two-the-command-line">Door two: the command line</h2>

<p>Browsers are for looking. For actually running things (kubectl, the Talos API), a
public URL is the wrong tool. I’d rather the cluster trust the device than check a
password.</p>

<p>So every node joins a private mesh with <a href="https://tailscale.com/">Tailscale</a>, and
my laptop is on the same mesh. The command line works from anywhere, like the
cluster is sitting on the desk next to me, and none of it is exposed to the public
internet.</p>

<p>This one cost me an afternoon and a few words I won’t print: the cluster’s API
refused to trust the mesh until I added the nodes’ mesh addresses to its
certificate. Until then, every connection got politely rejected. Add the
addresses, regenerate the cert, and suddenly we’re friends.</p>

<figure>
  <img src="https://media.giphy.com/media/v1.Y2lkPTc5MGI3NjExcmc5eXE0am1qYmRlOGs1ZW51MnpuMW9rdGtjN292bnhpeWpiZjl0bSZlcD12MV9naWZzX3NlYXJjaCZjdD1n/hFROvOhBPQVRm/giphy.gif" alt="reaction gif" loading="lazy" />
  <figcaption>kubectl over the tailnet, finally.</figcaption>
</figure>

<h2 id="two-doors-on-purpose">Two doors, on purpose</h2>

<p>The public browser door is for the quick “let me just check it” moments. The
private mesh door is for real admin work. Different trust, different jobs, and
neither one opens a hole in the home router, which was the whole point.</p>

<h2 id="whats-next">What’s next</h2>

<p>Right now the guard and the apps each ask for a login, so I sign in twice like
it’s 2009. Next up is making them agree: single sign-on with Authentik, so it’s
one face at the gate instead of two.</p>]]></content><author><name>Lalet Scaria</name></author><category term="homelab" /><category term="cloudflare" /><category term="tailscale" /><category term="networking" /><category term="security" /><summary type="html"><![CDATA[Part 4 got the cluster running itself, but it only lived on the home network. I wanted to check it from the couch, from my phone, from a cafe on borrowed wifi. So I set up two ways in, for two different jobs, and neither one opens a port on the home router. That last part is the whole reason I can sleep at night.]]></summary></entry><entry><title type="html">Homelab, Part 6: Watching It All</title><link href="https://lalet.me/pr-preview/pr-30/2026/06/24/homelab-part-6-observability.html" rel="alternate" type="text/html" title="Homelab, Part 6: Watching It All" /><published>2026-06-24T00:00:00+00:00</published><updated>2026-06-24T00:00:00+00:00</updated><id>https://lalet.me/pr-preview/pr-30/2026/06/24/homelab-part-6-observability</id><content type="html" xml:base="https://lalet.me/pr-preview/pr-30/2026/06/24/homelab-part-6-observability.html"><![CDATA[<p><a href="/pr-preview/pr-30/2026/06/24/homelab-part-5-remote-access.html">Part 5</a>
opened two doors into the cluster. Now I wanted to actually see what was going on
inside it: metrics, logs, network flows, a wall of graphs to stare at instead of
sleeping. This is where the homelab stops being a pile of YAML and starts telling
me how it feels.</p>

<h2 id="first-where-does-all-this-live">First, where does all this live?</h2>

<p>Metrics and logs are just data, and data needs somewhere to sit. There are two
kinds here, and they want different homes.</p>

<p>Time-series metrics act like a small, busy database, so they want fast block
storage that survives a reboot. That’s <a href="https://longhorn.io/">Longhorn</a>, which
keeps replicated block volumes spread across the worker nodes. A database that
loses its disk every time a node blinks isn’t really a database.</p>

<p>Logs and traces are happier in object storage, the S3 kind. The obvious pick was
<a href="https://min.io/">MinIO</a>, except the free community edition has gone quiet: no
real releases in months, features trimmed out. I didn’t feel like building on
something that already looks like it’s coasting.</p>

<figure>
  <img src="https://media.giphy.com/media/QMHoU66sBXqqLqYvGO/giphy.gif" alt="this is fine dog in a burning room" loading="lazy" />
  <figcaption>MinIO's community edition, reportedly doing great.</figcaption>
</figure>

<p>So I went with <a href="https://garagehq.deuxfleurs.fr/">Garage</a> instead. It’s small,
written in Rust, and copies objects across nodes by itself. That’s also why I run
it on plain local disk instead of on Longhorn: Garage already makes its own
replicas, so layering it on top of Longhorn would just pay for the same safety
twice.</p>

<h2 id="the-stack-victoriametrics-not-prometheus">The stack: VictoriaMetrics, not Prometheus</h2>

<p>For metrics I went with <a href="https://victoriametrics.com/">VictoriaMetrics</a> instead
of the usual Prometheus setup, mostly to save memory. Same query language, a
fraction of the RAM, and I only keep five days of history anyway. If I haven’t
looked at a graph within five days, I’m not going to.</p>

<p>Logs go through <a href="https://grafana.com/oss/loki/">Loki</a>, with
<a href="https://grafana.com/oss/alloy-opentelemetry-collector/">Grafana Alloy</a> as the
agent on every node. Alloy tails the pods, throws out the noise like health
checks and blank lines, keeps every error and warning, and samples the rest. I’d
rather not pay to store a million copies of <code class="language-plaintext highlighter-rouge">GET /healthz 200</code>.</p>

<p>The freebie I didn’t expect: because the cluster runs <a href="https://cilium.io/">Cilium</a>,
its <a href="https://github.com/cilium/hubble">Hubble</a> layer hands me network metrics for
almost nothing. There’s no proxy stapled to every pod. Cilium already lives down
in the kernel’s network path, so turning on metrics just exposes what it can
already see. One toggle and there were suddenly graphs of every connection, every
dropped packet, every DNS lookup in the cluster.</p>

<figure>
  <img src="https://media.giphy.com/media/26ufdipQqU2lhNA4g/giphy.gif" alt="mind blown reaction" loading="lazy" />
  <figcaption>Me, realizing the network data had been there all along.</figcaption>
</figure>

<p><a href="https://grafana.com/oss/grafana/">Grafana</a> sits on top and ties it together,
behind the same single login from Part 5.</p>

<h2 id="what-bit-me">What bit me</h2>

<p>None of it worked first try, obviously. A few favorites.</p>

<h3 id="the-missing-agent">The missing agent</h3>

<p>The node metrics exporter runs as a DaemonSet, so it should land one copy on
every node. Instead it landed on none. No pods, no crash, no error in any log,
just a count reading 0 of 7. Talos ships a strict pod security profile by
default, and that exporter wants host access the profile quietly denies. Nothing
says “I blocked this,” the pods just never get created. The fix was one label on
the namespace.</p>

<figure>
  <img src="https://media.giphy.com/media/hQKJwKAzq5NERlLf8L/giphy.gif" alt="confused math lady" loading="lazy" />
  <figcaption>0 of 7 pods and not a single error. Cool. Cool cool cool.</figcaption>
</figure>

<h3 id="the-63-character-wall">The 63-character wall</h3>

<p>One component refused to sync, grumbling that a name was too long. Kubernetes
caps certain names at 63 characters, and the chart had stacked its own name on
top of the one I gave it until the total tipped over. A shorter name override and
it stopped sulking.</p>

<h3 id="the-volume-tug-of-war">The volume tug-of-war</h3>

<p>Grafana jammed halfway through a restart, the new copy waiting forever to come
up. Its disk only attaches to one node at a time, but the default rollout tries
to start the new copy before the old one lets go. Both wanted the same volume and
neither would blink. I told it to kill the old copy first, and the standoff
ended.</p>

<h3 id="the-wrong-door">The wrong door</h3>

<p>A whole dashboard sat there reading “No data,” and I nearly went crawling through
the scrape config hunting for the leak. The metrics were flowing the entire time.
I was just asking the wrong port for them. The service answers on one number, and
I’d spent a while knocking politely on the one next door.</p>

<figure>
  <img src="https://media.giphy.com/media/HDQ26psL97H8K2qoUU/giphy.gif" alt="Picard facepalm" loading="lazy" />
  <figcaption>It was the port. It's always something like the port.</figcaption>
</figure>

<h2 id="while-i-was-in-there-cloudflare-as-code">While I was in there: Cloudflare as code</h2>

<p>Part 5 set up the tunnel and its access rules by clicking around a dashboard,
which always leaves a small itch. So I pulled the whole thing into
<a href="https://opentofu.org/">OpenTofu</a> and let code own it. The one rule when you adopt
live infrastructure: import, never recreate. Describe what already exists, nudge
the config until a plan shows zero changes, and only then trust it. Get that
wrong and you’ll happily tear down the working tunnel you were trying to save.</p>

<h2 id="whats-next">What’s next</h2>

<p>What I actually want out of all this: one login for everything, and a tidy page
of app tiles like a work SSO portal. That’s <a href="https://goauthentik.io/">Authentik</a>,
and it’s next.</p>]]></content><author><name>Lalet Scaria</name></author><category term="homelab" /><category term="kubernetes" /><category term="observability" /><category term="victoriametrics" /><category term="storage" /><summary type="html"><![CDATA[Part 5 opened two doors into the cluster. Now I wanted to actually see what was going on inside it: metrics, logs, network flows, a wall of graphs to stare at instead of sleeping. This is where the homelab stops being a pile of YAML and starts telling me how it feels.]]></summary></entry><entry><title type="html">Homelab, Part 4: GitOps with ArgoCD</title><link href="https://lalet.me/pr-preview/pr-30/2026/06/23/homelab-part-4-argocd.html" rel="alternate" type="text/html" title="Homelab, Part 4: GitOps with ArgoCD" /><published>2026-06-23T00:00:00+00:00</published><updated>2026-06-23T00:00:00+00:00</updated><id>https://lalet.me/pr-preview/pr-30/2026/06/23/homelab-part-4-argocd</id><content type="html" xml:base="https://lalet.me/pr-preview/pr-30/2026/06/23/homelab-part-4-argocd.html"><![CDATA[<p><a href="/pr-preview/pr-30/2026/06/22/homelab-part-3-kubernetes.html">Part 3</a> got
the Talos cluster running. Part 4 is how I stop touching it by hand.
<a href="https://argo-cd.readthedocs.io/en/stable/">ArgoCD</a> watches a Git repo and
reconciles the cluster to match it. I push, it deploys.</p>

<h2 id="one-repo-app-of-apps">One repo, App-of-Apps</h2>

<p>The repo (<code class="language-plaintext highlighter-rouge">argo-home</code>) is the source of truth. Change something live and ArgoCD
drifts it back; lose a node and the repo rebuilds it. I point ArgoCD at one
<a href="https://argo-cd.readthedocs.io/en/stable/operator-manual/applicationset/">ApplicationSet</a>
that turns each folder into a managed app, so adding a service is a pull request,
not a kubectl session.</p>

<p>ArgoCD even manages itself. The only manual step is the first install; after that
it adopts its own manifests and upgrades through Git. It needs
<a href="https://kubernetes.io/docs/reference/using-api/server-side-apply/">server-side apply</a>,
or the big CRD manifests blow past the annotation size limit.</p>

<p>Today it keeps a deliberately short list in sync: ArgoCD,
<a href="https://cilium.io/">Cilium</a>, <a href="https://cert-manager.io/">cert-manager</a>, and the
two storage drivers. A few solid apps beat a dashboard full of
half-broken ones.</p>

<h2 id="secrets-with-sops">Secrets with SOPS</h2>

<p>Secrets live in the repo too, encrypted with <a href="https://github.com/getsops/sops">SOPS</a>
and an <a href="https://github.com/FiloSottile/age">age</a> key, so what gets committed is
ciphertext. ArgoCD decrypts them at render time with
<a href="https://github.com/viaduct-ai/kustomize-sops">KSOPS</a>, a generator plugin in the
repo-server. The only thing not in Git is the age private
key itself, loaded into the cluster once by hand. Each encrypted secret carries
its own namespace, so one app fans them out to wherever they belong.</p>

<p>Two things ate an afternoon:</p>

<ul>
  <li>The KSOPS image has no shell, so an init container running <code class="language-plaintext highlighter-rouge">/bin/sh -c</code> just
crash-loops. Call the binary directly.</li>
  <li>ArgoCD’s command-params config map doesn’t restart its pod when it changes.
Restart it yourself, or run <a href="https://github.com/stakater/Reloader">Stakater Reloader</a>.</li>
</ul>

<p>One catch: ArgoCD caches rendered manifests, decrypted secrets included, in Redis
as plaintext. The project discourages it for exactly that reason. Fine for a
private one-person cluster, not for anywhere with real blast radius.</p>

<h2 id="what-else-bit-me">What else bit me</h2>

<p>I started with <a href="https://metallb.universe.tf/">MetalLB</a> and pulled it out once Cilium handled load balancing. And
on a fresh cluster, ApplicationSet order matters: namespaces have to exist before
the apps that land in them.</p>

<h2 id="next">Next</h2>

<p>The cluster runs itself, but I still reach it over the local network. Part 5 is
exposing things properly: single sign-on with
<a href="https://goauthentik.io/">Authentik</a>, and getting in from outside without opening
the router.</p>]]></content><author><name>Lalet Scaria</name></author><category term="homelab" /><category term="kubernetes" /><category term="argocd" /><category term="gitops" /><summary type="html"><![CDATA[Part 3 got the Talos cluster running. Part 4 is how I stop touching it by hand. ArgoCD watches a Git repo and reconciles the cluster to match it. I push, it deploys.]]></summary></entry><entry><title type="html">Homelab, Part 3: A Kubernetes Cluster on Talos</title><link href="https://lalet.me/pr-preview/pr-30/2026/06/22/homelab-part-3-kubernetes.html" rel="alternate" type="text/html" title="Homelab, Part 3: A Kubernetes Cluster on Talos" /><published>2026-06-22T10:00:00+00:00</published><updated>2026-06-22T10:00:00+00:00</updated><id>https://lalet.me/pr-preview/pr-30/2026/06/22/homelab-part-3-kubernetes</id><content type="html" xml:base="https://lalet.me/pr-preview/pr-30/2026/06/22/homelab-part-3-kubernetes.html"><![CDATA[<p><a href="/pr-preview/pr-30/2026/06/21/homelab-part-1-hardware.html">Part 1</a> covered
the hardware and <a href="/pr-preview/pr-30/2026/06/22/homelab-part-2-proxmox.html">Part 2</a>
got Proxmox running. This is where it comes together: a
<a href="https://kubernetes.io/">Kubernetes</a> cluster spread across the Proxmox VMs and
the Raspberry Pis, running <a href="https://www.talos.dev/">Talos Linux</a>.</p>

<h2 id="why-talos">Why Talos</h2>

<p>I didn’t want to hand-maintain a control plane. Talos is a Linux built only for
Kubernetes, with no SSH and no shell to log into. You don’t manage a node by
poking at it, you declare its config and apply it over an API. That sounds
restrictive, and that’s the point: the whole cluster lives in config files, so
rebuilding a node is boring and repeatable instead of a one-off.</p>

<h2 id="the-cluster">The cluster</h2>

<p>Seven nodes, mixed on purpose:</p>

<table>
  <thead>
    <tr>
      <th>Role</th>
      <th>Nodes</th>
      <th>Where</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>control plane</td>
      <td>cp1, cp2</td>
      <td>Proxmox VMs (amd64)</td>
    </tr>
    <tr>
      <td>control plane</td>
      <td>cp3</td>
      <td>Raspberry Pi (arm64)</td>
    </tr>
    <tr>
      <td>worker</td>
      <td>worker1, worker2, worker3</td>
      <td>Proxmox VMs (amd64)</td>
    </tr>
    <tr>
      <td>worker</td>
      <td>worker4</td>
      <td>Raspberry Pi (arm64)</td>
    </tr>
  </tbody>
</table>

<p>Three control planes, not two. Part 2 ended on the two-node quorum problem, and
this is the fix: <a href="https://etcd.io/">etcd</a> wants an odd number, so a third control plane (a Pi) lets
the cluster survive losing any one of them. The control planes sit on different
machines, and a floating virtual IP is the API endpoint, so kubectl talks to the
cluster rather than to one node. The amd64 VMs and the arm64 Pis run side by
side, which keeps me honest about multi-arch images.</p>

<h2 id="the-troubles">The troubles</h2>

<p>A few things cost me real time.</p>

<p><a href="https://cilium.io/">Cilium</a> dropped traffic between nodes. With the default overlay, pods on the same
node could talk but pods on different nodes couldn’t, and it failed silently. On
Proxmox the fix was switching Cilium to native routing.</p>

<p>The interface name matters. Talos pins an address to a named NIC, and the name
isn’t the same on the VMs as it is on the Pi. Point the config at the wrong one
and the address just never comes up.</p>

<p>There’s also a bootstrap catch-22: Cilium needs the API to start, but the API’s
floating IP doesn’t exist until the control plane is already up. You point Cilium
at a real node to bootstrap, then move it to the floating IP once things settle.</p>

<h2 id="storage">Storage</h2>

<p>Two storage classes. NFS to the Buffalo NAS for bulk and shared data, and
local-path on the SSDs for anything that needs to be quick.</p>

<h2 id="what-runs-on-it">What runs on it</h2>

<p>Everything is GitOps through <a href="https://argo-cd.readthedocs.io/en/stable/">ArgoCD</a>,
so the cluster’s state is just a Git repo. On top of that sits the reason for the
whole upgrade: <a href="https://ollama.com/">Ollama</a> for local LLMs, and
Plex with the iGPU passed through for transcoding.</p>

<h2 id="whats-next">What’s next</h2>

<p>The cluster is up and mostly runs itself. Part 4 gets into the GitOps setup: how
ArgoCD bootstraps everything, how secrets are handled, and how I reach services
with single sign-on.</p>

<p>Seven nodes, two architectures, one kubectl away. It’s all learning, and the
stage just got bigger.</p>]]></content><author><name>Lalet Scaria</name></author><category term="homelab" /><category term="kubernetes" /><category term="talos" /><category term="cilium" /><category term="gitops" /><summary type="html"><![CDATA[Part 1 covered the hardware and Part 2 got Proxmox running. This is where it comes together: a Kubernetes cluster spread across the Proxmox VMs and the Raspberry Pis, running Talos Linux.]]></summary></entry><entry><title type="html">Homelab, Part 2: Installing Proxmox</title><link href="https://lalet.me/pr-preview/pr-30/2026/06/22/homelab-part-2-proxmox.html" rel="alternate" type="text/html" title="Homelab, Part 2: Installing Proxmox" /><published>2026-06-22T00:00:00+00:00</published><updated>2026-06-22T00:00:00+00:00</updated><id>https://lalet.me/pr-preview/pr-30/2026/06/22/homelab-part-2-proxmox</id><content type="html" xml:base="https://lalet.me/pr-preview/pr-30/2026/06/22/homelab-part-2-proxmox.html"><![CDATA[<p><a href="/pr-preview/pr-30/2026/06/21/homelab-part-1-hardware.html">Part 1</a> covered
the hardware. This one is about <a href="https://www.proxmox.com/en/proxmox-virtual-environment/overview">Proxmox VE</a>, the thing that turns those two mini
PCs into boxes I can carve VMs out of. Both nodes run version 9.</p>

<h2 id="installing-it">Installing it</h2>

<p>Flash the Proxmox ISO to a USB stick, boot the OptiPlex from it, and click
through the installer. The only real stop is the BIOS, where you turn on hardware
virtualization (VT-x/VT-d) so VMs can actually run.</p>

<h2 id="the-subscription-repo">The subscription repo</h2>

<p>Proxmox ships with its enterprise package repo enabled, and that one needs a paid
subscription. So your first <code class="language-plaintext highlighter-rouge">apt update</code> fails with a 401, and the web UI nags
you about it on every login. The fix is to switch to the free
<code class="language-plaintext highlighter-rouge">pve-no-subscription</code> repo and update. For a homelab that is the right call: same
packages, no support contract.</p>

<h2 id="storage">Storage</h2>

<p>The stock 256 GB drive is fine for Proxmox itself but fills up fast once VMs pile
on, so each node got a 2 TB SSD for the VM disks.</p>

<table>
  <thead>
    <tr>
      <th>Storage</th>
      <th>Backed by</th>
      <th>Holds</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>local</td>
      <td>256 GB NVMe</td>
      <td>ISOs, templates, backups</td>
    </tr>
    <tr>
      <td>local-lvm</td>
      <td>256 GB NVMe</td>
      <td>small system disks</td>
    </tr>
    <tr>
      <td>local-ssd</td>
      <td>2 TB SSD</td>
      <td>the actual VM disks</td>
    </tr>
  </tbody>
</table>

<p>I used LVM-thin rather than <a href="https://openzfs.org/">ZFS</a>. These are single-disk
nodes without much spare RAM, and ZFS wants more of both than this setup has to
give.</p>

<h2 id="networking">Networking</h2>

<p>Proxmox creates a bridge (<code class="language-plaintext highlighter-rouge">vmbr0</code>) for the VMs, and I put it on the wired NIC
with a static address. The OptiPlex has Wi-Fi too, but Wi-Fi and bridged VMs
don’t get along, so everything stays on Ethernet.</p>

<h2 id="to-cluster-or-not">To cluster or not</h2>

<p>Proxmox can pool nodes into a cluster, but mine run as two standalone installs.
Two-node clusters are the awkward case: the quorum vote wants an odd number, so
two nodes really need a third tie-breaker before one rebooting takes the whole
thing down. I didn’t want that babysitting, and the Kubernetes layer (next post)
gives me high availability above Proxmox anyway.</p>

<h2 id="what-bit-me">What bit me</h2>

<ul>
  <li>The enterprise-repo 401 on the first update. Obvious once you know, confusing
the first time.</li>
  <li>Forgetting the BIOS virtualization toggle before VMs will start.</li>
  <li>The stock drive is too small on its own. Budget for the extra SSD from day one.</li>
</ul>

<h2 id="whats-next">What’s next</h2>

<p>Proxmox is installed and the network is bridged. Part 3 is the part I actually
wanted to build: a Kubernetes cluster spread across both nodes and the Pis.</p>]]></content><author><name>Lalet Scaria</name></author><category term="homelab" /><category term="proxmox" /><category term="infrastructure" /><summary type="html"><![CDATA[Part 1 covered the hardware. This one is about Proxmox VE, the thing that turns those two mini PCs into boxes I can carve VMs out of. Both nodes run version 9.]]></summary></entry><entry><title type="html">Homelab, Part 1: Setting Up the Hardware</title><link href="https://lalet.me/pr-preview/pr-30/2026/06/21/homelab-part-1-hardware.html" rel="alternate" type="text/html" title="Homelab, Part 1: Setting Up the Hardware" /><published>2026-06-21T00:00:00+00:00</published><updated>2026-06-21T00:00:00+00:00</updated><id>https://lalet.me/pr-preview/pr-30/2026/06/21/homelab-part-1-hardware</id><content type="html" xml:base="https://lalet.me/pr-preview/pr-30/2026/06/21/homelab-part-1-hardware.html"><![CDATA[<p>First post in a series on building out my homelab. The goal is to run the same
kind of infrastructure I work on (<a href="https://kubernetes.io/">Kubernetes</a>, GitOps, observability) without
paying a cloud bill for every experiment. Part 1 is the hardware.</p>

<h2 id="how-it-started">How it started</h2>

<p>I ran a homelab on a couple of Raspberry Pis for a while. They were great for
poking at things: small, cheap, low power, and hard to break in any permanent
way.</p>

<p>Then I wanted to run models locally with <a href="https://ollama.com/">Ollama</a>, and the Pis ran out of room
fast. Fine for tinkering, not for anything memory hungry. I wanted a real
multi-node setup I could run a proper Kubernetes cluster on, and host local LLMs
without paying per token.</p>

<p>So I went shopping, secondhand and cheap. That budget is the whole reason this is
mini PCs and a few small boxes instead of a loud rack server.</p>

<h2 id="the-hardware">The hardware</h2>

<p>Four small machines, a switch, and a NAS. Nothing rack mounted.</p>

<h3 id="compute-2x-dell-optiplex-3070-micro">Compute: 2x Dell OptiPlex 3070 Micro</h3>

<p>The two workhorses, plex1 and plex2. They’re identical:</p>

<table>
  <thead>
    <tr>
      <th>Component</th>
      <th>Spec (per node)</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>CPU</td>
      <td>Intel Core i5-8500T (6 cores, 35 W)</td>
    </tr>
    <tr>
      <td>RAM</td>
      <td>24 GB</td>
    </tr>
    <tr>
      <td>Boot disk</td>
      <td>~256 GB NVMe</td>
    </tr>
    <tr>
      <td>Data disk</td>
      <td>2 TB SSD</td>
    </tr>
    <tr>
      <td>Hypervisor</td>
      <td><a href="https://www.proxmox.com/en/proxmox-virtual-environment/overview">Proxmox VE</a> 9</td>
    </tr>
  </tbody>
</table>

<p>A 35 W CPU sips power and stays quiet, but six cores and 24 GB are plenty for a
few VMs, with enough headroom left for Ollama. I picked two boxes instead of one
bigger one on purpose, so I could split the cluster across machines and actually
learn how multi-node setups behave.</p>

<h3 id="2x-raspberry-pi-5">2x Raspberry Pi 5</h3>

<p>The Pis are still here, and they’re not just tinkering boxes anymore. Both run
<a href="https://www.talos.dev/">Talos Linux</a> as full members of the Kubernetes cluster (Part 3 covers that). One
is a third control-plane node, the tie-breaker that gives etcd an odd-numbered
quorum. The other is an arm64 worker. A Pi makes a good tie-breaker: low power,
and unlike a laptop it comes back on by itself after an outage.</p>

<h3 id="storage-buffalo-terastation-5200-nvr">Storage: Buffalo TeraStation 5200 NVR</h3>

<p>Bulk storage and backups live on a secondhand Buffalo TeraStation, a 2-bay box
with two 4 TB drives. It has gigabit networking, so the box itself isn’t the
limit, but it’s slower than the local SSDs, so it stays in the backups tier.
Anything that needs to be quick lives on the node-local SSDs instead.</p>

<h3 id="networking-tp-link-switch">Networking: TP-Link switch</h3>

<p>Everything sits on one flat LAN:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>                Internet
                   |
            home router   (gateway, DHCP)
                   |
            TP-Link switch  (unmanaged, 5 V)
        |       |        |        |       |
      plex1   plex2   Buffalo    rpi5    rpi5
                       NAS
</code></pre></div></div>

<p>It’s a plain unmanaged 5 V TP-Link unit, the same low-power class as the Pis. No
VLANs, nothing fancy. The OptiPlex boxes each have one gigabit NIC, and the
switch just ties everything together off the router. VLANs are on the list for a
later post.</p>

<h3 id="power">Power</h3>

<p>Low power by design, since those 35 W CPUs idle quietly. No UPS yet. That’s the
next buy, because a power blip is the one thing this setup can’t ride out right
now.</p>

<h2 id="whats-running-on-it">What’s running on it</h2>

<p>The Kubernetes cluster runs as VMs on the two Proxmox nodes, plus the two Pis.
The Proxmox side:</p>

<table>
  <thead>
    <tr>
      <th>VM</th>
      <th>Role</th>
      <th>Host</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>cp1</td>
      <td>control plane</td>
      <td>plex1</td>
    </tr>
    <tr>
      <td>cp2</td>
      <td>control plane</td>
      <td>plex2</td>
    </tr>
    <tr>
      <td>worker1</td>
      <td>worker</td>
      <td>plex1</td>
    </tr>
    <tr>
      <td>worker2</td>
      <td>worker</td>
      <td>plex2</td>
    </tr>
    <tr>
      <td>worker3</td>
      <td>worker</td>
      <td>plex2</td>
    </tr>
  </tbody>
</table>

<p>The two Pis add a third control plane and a fourth worker, so the full cluster is
three control planes and four workers. On top of it runs my GitOps stack and the
local-AI experiments that kicked off the whole upgrade.</p>

<h2 id="whats-next">What’s next</h2>

<p>Part 2 covers Proxmox: installing it on the two OptiPlex boxes and getting them
ready to run the cluster.</p>

<p>It’s all learning, and the whole world’s our stage.</p>]]></content><author><name>Lalet Scaria</name></author><category term="homelab" /><category term="infrastructure" /><category term="proxmox" /><category term="kubernetes" /><summary type="html"><![CDATA[First post in a series on building out my homelab. The goal is to run the same kind of infrastructure I work on (Kubernetes, GitOps, observability) without paying a cloud bill for every experiment. Part 1 is the hardware.]]></summary></entry></feed>